$("#follow_user_form").html("<%= escape_javascript(render('users/follow_user')) %>")
<% @boxes.each do | box | %>
$("#unfollow_box_<%= box.id %>").html("<%= escape_javascript(render('boxes/follow_box', box: box)) %>")
<% end %>