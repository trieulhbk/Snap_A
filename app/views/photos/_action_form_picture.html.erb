<div>
		<%= link_to "Repin", edit_box_path(@box)%>
		<%= link_to "Like", edit_box_path(@box)%>
		<%= render 'photos/like_photo_form', photo: photo %>
		<%= link_to "Comment", edit_box_path(@box)%>
</div>