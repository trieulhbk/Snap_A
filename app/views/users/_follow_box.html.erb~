
<div id="follow_form">
	Follow_form
	<%if current_user.following_box?(box) %>	
	<div> unfollow box</div>
	<%= render 'unfollow_box', box: box %>
	<% else %>
	<%= render 'follow_box', box: box %>
	<div> follow box</div>
	<% end %>
</div>
