<%= form_for(current_user.boxes.build(), remote: true) do |f| %>
 	
      <%= f.label :name %>
      <%= f.text_field :name, placeholder: "Board Name" %>
      <%= f.submit "Create board", class: "btn btn-large btn-primary" %>
<% end %>	
